import ChatInterface from "@/components/ChatInterface";

export const metadata = {
  title: "AI Chat - Claude & Gemini",
  description: "Chat with Claude or Gemini AI models",
};

export default function ChatPage() {
  return <ChatInterface />;
}
